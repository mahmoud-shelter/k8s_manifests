apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wee-in-admin-dashboard-dev
  namespace: dev
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    service.beta.kubernetes.io/aws-load-balancer-proxy-protocol: "*"
spec:
  ingressClassName: nginx
  rules:
   - host: wee-admin.shelter-technology.com
     http:
      paths:
      - path: /*
        pathType: Prefix
        backend:
          service:
            name: wee-in-admin-dashboard-dev
            port:
              number: 80