apiVersion: v1
kind: Service
metadata:
  name: wee-in-backend-dev
  namespace: dev
  labels:
    service: wee-in-backend-dev
spec:
  type: ClusterIP
  selector:
    app: wee-in-backend-dev
  ports:
  - port: 80         # ClusterIP port
    targetPort: 8000   # Pod port