apiVersion: v1
kind: Service
metadata:
  name: nos-tracking-website-stg
  namespace: dev
  labels:
    service: nos-tracking-website-stg
spec:
  type: ClusterIP
  selector:
    app: nos-tracking-website-stg
  ports:
  - port: 80         # ClusterIP port
    targetPort: 3000   # Pod port