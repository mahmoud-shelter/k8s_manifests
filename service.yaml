apiVersion: v1
kind: Service
metadata:
  name: wee-website-stg
  namespace: dev
  labels:
    service: wee-website-stg
spec:
  type: ClusterIP
  selector:
    app: wee-website-stg
  ports:
  - port: 80         # ClusterIP port
    targetPort: 3000   # Pod port