apiVersion: v1
kind: Service
metadata:
  name: absi-backend-prod
  namespace: dev
  labels:
    service: absi-backend-prod
spec:
  type: ClusterIP
  selector:
    app: absi-backend-prod
  ports:
  - port: 80         # ClusterIP port
    targetPort: 3007   # Pod port




