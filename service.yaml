apiVersion: v1
kind: Service
metadata:
  name: nos-nuxt-website-prod
  namespace: dev
  labels:
    service: nos-nuxt-website-prod
spec:
  type: ClusterIP
  selector:
    app: nos-nuxt-website-prod
  ports:
  - port: 80         # ClusterIP port
    targetPort: 3000   # Pod port




