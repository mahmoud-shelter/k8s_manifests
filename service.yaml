apiVersion: v1
kind: Service
metadata:
  name: wee-in-backend-stg
  namespace: dev
  labels:
    service: wee-in-backend-stg
spec:
  type: ClusterIP
  selector:
    app: wee-in-backend-stg
  ports:
  - port: 80         # ClusterIP port
    targetPort: 8000   # Pod port