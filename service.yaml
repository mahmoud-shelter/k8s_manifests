apiVersion: v1
kind: Service
metadata:
  name: nos-dashboard-react-prod
  namespace: dev
  labels:
    service: nos-dashboard-react-prod
spec:
  type: ClusterIP
  selector:
    app: nos-dashboard-react-prod
  ports:
  - port: 80         # ClusterIP port
    targetPort: 3000   # Pod port