apiVersion: v1
kind: Service
metadata:
  name: nos-nuxt-website-dev
  namespace: dev
  labels:
    service: nos-nuxt-website-dev
spec:
  type: ClusterIP
  selector:
    app: nos-nuxt-website-dev
  ports:
  - port: 80         # ClusterIP port
    targetPort: 3000   # Pod port




