apiVersion: v1
kind: Service
metadata:
  name: nos-backend-service-dev
  namespace: dev
  labels:
    service: nos-backend-service-dev
spec:
  type: ClusterIP
  selector:
    app: nos-backend-service-dev
  ports:
  - port: 80         # ClusterIP port
    targetPort: 8000   # Pod port