apiVersion: v1
kind: Service
metadata:
  name: la7-website-stg
  namespace: dev
  labels:
    service: la7-website-stg
spec:
  type: ClusterIP
  selector:
    app: la7-website-stg
  ports:
  - port: 80         # ClusterIP port
    targetPort: 3000   # Pod port