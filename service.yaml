apiVersion: v1
kind: Service
metadata:
  name: absi-backend-stg
  namespace: dev
  labels:
    service: absi-backend-stg
spec:
  type: ClusterIP
  selector:
    app: absi-backend-stg
  ports:
  - port: 80         # ClusterIP port
    targetPort: 8000   # Pod port