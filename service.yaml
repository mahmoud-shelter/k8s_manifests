apiVersion: v1
kind: Service
metadata:
  name: nos-celery-flower
  namespace: dev
  labels:
    service: nos-celery-flower
spec:
  type: ClusterIP
  selector:
    app: nos-celery-flower
  ports:
  - port: 80         # ClusterIP port
    targetPort: 5555   # Pod port