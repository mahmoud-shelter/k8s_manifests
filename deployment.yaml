apiVersion: apps/v1
kind: Deployment
metadata:
  name: wee-website-dev-deployment
  namespace: dev
  labels:
    app: wee-website-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wee-website-dev
  template:
    metadata:
      labels:
        app: wee-website-dev
    spec:
      containers:
      - name: wee-website-dev
        image: IMAGE_NAME
        ports:
        - containerPort: 3000
        envFrom:
        - secretRef:
            name: wee-website-dev
