apiVersion: apps/v1
kind: Deployment
metadata:
  name: umin-sql-prod
  namespace: prod
  labels:
    app: umin-sql-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: umin-sql-prod
  template:
    metadata:
      labels:
        app: umin-sql-prod
    spec:
      containers:
        - name: umin-sql-prod
          image: IMAGE_NAME
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: umin-sql-prod
          resources:
            requests:
              memory: "4Gi"
              cpu: "1"
            limits: 
              memory: "4Gi"
              cpu: "2"
      tolerations:
        - key: "ENV"
          operator: "Equal"
          value: "PROD"
          effect: "NoSchedule"

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: umin-sql-prod-hpa
  namespace: dev
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: umin-sql-prod
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: AverageValue
          averageValue: 3Gi