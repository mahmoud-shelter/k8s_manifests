apiVersion: apps/v1
kind: Deployment
metadata:
  name: wee-in-admin-dashboard-dev-deployment
  namespace: dev
  labels:
    app: wee-in-admin-dashboard-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wee-in-admin-dashboard-dev
  template:
    metadata:
      labels:
        app: wee-in-admin-dashboard-dev
    spec:
      containers:
      - name: wee-in-admin-dashboard-dev
        image: IMAGE_NAME
        ports:
        - containerPort: 3000
        envFrom:
        - secretRef:
            name: wee-in-admin-dashboard-dev
