apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-radar-gateway-service-deployment
  namespace: dev
  labels:
    app: platform-radar-gateway-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: platform-radar-gateway-service
  template:
    metadata:
      labels:
        app: platform-radar-gateway-service
    spec:
      containers:
        - name: platform-radar-gateway-service
          image: IMAGE_NAME
          ports:
            - containerPort: 3003
          envFrom:
            - secretRef:
                name: platform-radar-gateway-service
