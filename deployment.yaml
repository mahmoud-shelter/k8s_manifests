apiVersion: apps/v1
kind: Deployment
metadata:
  name: nos-tracking-website-stg-deployment
  namespace: dev
  labels:
    app: nos-tracking-website-stg
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nos-tracking-website-stg
  template:
    metadata:
      labels:
        app: nos-tracking-website-stg
    spec:
      containers:
      - name: nos-tracking-website-stg
        image: IMAGE_NAME
        ports:
        - containerPort: 3000
        envFrom:
        - secretRef:
            name: nos-tracking-website-stg
