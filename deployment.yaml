apiVersion: apps/v1
kind: Deployment
metadata:
  name: nos-backend-service-dev-deployment
  namespace: dev
  labels:
    app: nos-backend-service-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nos-backend-service-dev
  template:
    metadata:
      labels:
        app: nos-backend-service-dev
    spec:
      containers:
      - name: nos-backend-service-dev
        image: IMAGE_NAME
        ports:
        - containerPort: 8000
        envFrom:
        - secretRef:
            name: nos-backend-service-dev
