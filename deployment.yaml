apiVersion: apps/v1
kind: Deployment
metadata:
  name: la7-backend-stg-deployment
  namespace: dev
  labels:
    app: la7-backend-stg
spec:
  replicas: 1
  selector:
    matchLabels:
      app: la7-backend-stg
  template:
    metadata:
      labels:
        app: la7-backend-stg
    spec:
      containers:
      - name: la7-backend-stg
        image: IMAGE_NAME
        ports:
        - containerPort: 8000
        envFrom:
        - secretRef:
            name: la7-backend-stg
