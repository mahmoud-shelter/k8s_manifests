apiVersion: apps/v1
kind: Deployment
metadata:
  name: nos-celery-flower
  namespace: dev
  labels:
    app: nos-celery-flower
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nos-celery-flower
  template:
    metadata:
      labels:
        app: nos-celery-flower
    spec:
      containers:
      - name: nos-celery-flower
        image: mher/flower
        ports:
        - containerPort: 5555
        env:
        - name: CELERY_BROKER_URL
          value: "redis://nos-redis.dev.svc.cluster.local:6379/0"