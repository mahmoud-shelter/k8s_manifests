apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-prosecution-service-deployment
  namespace: dev
  labels:
    app: platform-prosecution-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: platform-prosecution-service
  template:
    metadata:
      labels:
        app: platform-prosecution-service
    spec:
      containers:
        - name: platform-prosecution-service
          image: IMAGE_NAME
          ports:
            - containerPort: 3004
          envFrom:
            - secretRef:
                name: platform-prosecution-service
