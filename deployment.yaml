apiVersion: apps/v1
kind: Deployment
metadata:
  name: wee_in_backend-dev-deployment
  namespace: dev
  labels:
    app: wee_in_backend-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wee_in_backend-dev
  template:
    metadata:
      labels:
        app: wee_in_backend-dev
    spec:
      containers:
      - name: wee_in_backend-dev
        image: IMAGE_NAME
        ports:
        - containerPort: 8000
        envFrom:
        - secretRef:
            name: wee_in_backend-dev
