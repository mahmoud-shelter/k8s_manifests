apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-notification-service
  namespace: dev
  labels:
    app: platform-notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: platform-notification-service
  template:
    metadata:
      labels:
        app: platform-notification-service
    spec:
      containers:
        - name: platform-notification-service
          image: IMAGE_NAME
          ports:
            - containerPort: 3005
          envFrom:
            - secretRef:
                name: platform-notification-service
